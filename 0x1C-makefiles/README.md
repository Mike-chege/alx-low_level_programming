## :file_folder: Makefiles

This project covers Makefiles. That is how to make them using bash and use them to compile c programs.

## :open_file_folder: Compile with:
```
$ gcc -Wall -pedantic -Werror -Wextra main.c filename.c -o executable
```
